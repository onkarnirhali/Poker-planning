# PostgreSQL (adjust if you use Docker/Compose names)
DATABASE_URL=postgres://pp_user:pp_pass@localhost:5432/pokerplanning_dev

# Redis
REDIS_URL=redis://localhost:6379

# JWT secrets
JWT_SECRET=supersecuredevsecret
ACCESS_TOKEN_EXPIRY=1h
REFRESH_TOKEN_EXPIRY=7d

# Server port
PORT=4000